[[steps]]
name = "Run tests"
command = [
  "fluentci run --wasm cmake generate",
  "fluentci run --wasm cmake test",
]

[[steps]]
name = "Build"
command = [
  "fluentci run --wasm cmake generate",
  "fluentci run --wasm cmake make",
]
